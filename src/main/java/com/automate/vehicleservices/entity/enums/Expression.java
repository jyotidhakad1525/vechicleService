package com.automate.vehicleservices.entity.enums;

/**
 * @author Chandrashekar V
 * <p>
 * Use Ordinal to compare. before is first in time frame while after comes later. Similar approach needs to be followed
 * when declaring new enum instances here.
 */
public enum Expression {
    BEFORE,
    AFTER
}
